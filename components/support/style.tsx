import { styled } from '@mui/material';
import { CardWrapper } from '../common/ui';

const FullHeightCardWrapper = styled(CardWrapper)(({ theme }) => ({
  height: '100%',
}));

export { FullHeightCardWrapper };
