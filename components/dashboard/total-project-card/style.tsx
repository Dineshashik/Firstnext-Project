import { Stack, styled } from '@mui/material';

const TotalProjectDoughnutWrapper = styled(Stack)(({ theme }) => ({
  height: '166px',
  alignItems: 'center',
}));

export { TotalProjectDoughnutWrapper };
